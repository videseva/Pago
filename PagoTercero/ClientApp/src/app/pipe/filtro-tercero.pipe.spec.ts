import { FiltroTerceroPipe } from './filtro-tercero.pipe';

describe('FiltroTerceroPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTerceroPipe();
    expect(pipe).toBeTruthy();
  });
});
